node_modules/
dist/
coverage/
archived/

# This repository dogfoods its own decision records: `adr/` is tracked so the
# records are durable and shared. Drafts under `adr/.drafts/` stay ignored
# through `adr/.gitignore`.
*.tgz
.DS_Store
*.log

.idea/
.workbuddy/

# Generated agent integration target (adrkit init/update) plus local-only
# skills; the committed sources live in skills/.
.agents/
