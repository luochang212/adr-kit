# OpenADR skills

Agent skills for the OpenADR CLI. Each skill is a short, self-contained
workflow that drives the `openadr` binary.

Install into any skills-compatible agent by copying the `openadr-*/`
directories, or read them as the canonical agent instructions for the
matching command.
