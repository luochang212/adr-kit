---
"@luochang212/openadr": minor
---

Added `status` and `instructions` agent commands, `--all` flag for
`validate`, agent skills under `skills/`, bilingual docs, a changesets
release workflow, and repository governance files.
