## 1. Spec sync

- [x] 1.1 Add the `raised-by` requirements to `openspec/specs/decision-provenance/spec.md` and verify `openspec validate --specs` passes
- [x] 1.2 Correct the `decision-provenance` Purpose so "who proposed" is no longer body-only
- [x] 1.3 Add the `deliberation-tree` capability spec and verify `openspec validate --specs` passes
- [x] 1.4 Archive this change and verify `openspec list` reports no active changes

## 2. Cross-check

- [x] 2.1 Verify `npm test` and `adrkit validate --all` still pass
